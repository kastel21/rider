package com.operations.rider

/** SharedPreferences keys for embedded shell + landing sync. */
object OpsPrefs {
    const val NAME = "ops"
    const val KEY_LAST_SYNC_OK = "last_sync_ok"
    const val KEY_LAST_SYNC_AT = "last_sync_at"
}
